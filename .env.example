VITE_API_URL=http://localhost:5000
VITE_TEASER_MODE=false

# Base URL embedded in QR codes shown to customers (must be reachable from a phone).
# Leave empty in dev (falls back to window.location.origin = localhost), set in prod.
VITE_PUBLIC_URL=
